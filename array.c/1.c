#include <stdio.h>
int main()
{
  Printf("welcome to smallest age calculator");
  printf("enter the age of the different person");
  int age[n];
  for(i=0;i<n;i++)
  {
    scanf("%d",&age[i]);
  }
  for(i=0;i<n;i++)
  {
  printf("given age of different person is %d",age[i]);
  }
  int smallestage=age[0];
  if(smallestage > age[i]){
   smallestage=age[i];

  }
  printf("smallest age is %d",smallestage);
  return 0;
}